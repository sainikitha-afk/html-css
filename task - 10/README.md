# HTML & CSS — Frontend Assignment

A collection of frontend tasks.
Each task is self-contained in its own folder with an `index.html` and `styles.css`.

## Live Demo

> GitHub Pages serves from the root — open individual tasks via the links below once hosted.

---

## Tasks

| #  | Task | Concepts | Status |
|----|------|----------|--------|
| 10 | [Multi-Page Website Simulator](./task-10/) | `:target`, Grid, Flexbox, `@keyframes`, Transitions, Responsive | ✅ Done |

---

## Folder Structure
```
html-css/
└── task-10/
    ├── index.html
    └── styles.css
```

## How to View Locally

Just open any `index.html` directly in your browser — no build step, no dependencies, pure HTML & CSS.

---

*Built with zero JavaScript. Every interaction — page transitions, accordion, navigation — is pure CSS.*